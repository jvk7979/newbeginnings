// Goal Seek — placeholder. Replaced in the next commit with the
// bisection-backed solver UI.
export default function GoalSeek() {
  return (
    <div style={{ fontFamily: "'DM Sans', sans-serif", fontSize: 13, padding: '12px 0', color: '#888' }}>
      Goal Seek — coming up next.
    </div>
  );
}
